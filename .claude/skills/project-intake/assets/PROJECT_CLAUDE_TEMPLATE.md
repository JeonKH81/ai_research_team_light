# {P0N} — {짧은 제목}

이 폴더는 AI Research Team의 프로젝트 {P0N}이다. 작업 전 `PROJECT.md`를 먼저 읽어라 — 연구 질문·설계·현재 단계·차단 요인이 거기 있다.

팀 정의는 상위 `040_AI_Research_Team/`에 있다. 이 프로젝트를 실행할 때는 `research-team-orchestrator`, 단계 판단은 `lead-runbook`(유형: {type})을 사용하라.

**이 프로젝트의 불변 규칙:** {임의로 바꾸면 안 되는 범위·설계 결정 2~3개. 없으면 이 줄을 지운다}
