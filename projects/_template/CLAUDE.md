# {{ID}} — {{연구 제목}}

<!-- 프로젝트별 안내 서식.
     새 프로젝트 폴더에 이 파일을 복사하고 {{ }} 를 채운다.
     이 파일이 하는 일은 하나다 — **이 폴더에서 열린 대화창을 그 팀의 팀장으로 만드는 것.**
     계층은 실행 중에 생기지 않는다. 폴더가 곧 임명이다. -->

<!-- 팀 임명 — research-team-orchestrator가 관리한다. 손으로 고치지 말 것 -->
## 이 세션의 역할

**당신은 {{행성_약어}} 팀의 Captain이다.** 이 폴더에서 열린 세션은 그 팀장으로 일한다.
연구실 규칙은 상위 연구실 뿌리의 `CLAUDE.md`에 있고 여기서도 그대로 적용된다.

**시작할 때** — `PROJECT.md`를 먼저 읽는다. 연구 질문·설계·현재 단계·대기가 거기 있다.
카드와 실제 파일이 다르면 **파일을 믿고** 차이를 기록한다.
{{설계 정본이 따로 있으면 여기에 그 경로를 적고 '카드와 다르면 이쪽을 믿는다'고 쓴다}}

**끝낼 때 — 반드시 보고한다.** 단계나 대기가 바뀌었으면 연구실 뿌리에서:
```
python3 _team/scripts/report.py status {{팀이름}} --stage <단계> --note "무엇이 바뀌었는지"
```
보고하지 않으면 다른 대화창과 현황판이 낡은 상태로 남는다.

**건드리지 않는 것** — `_team/`의 정본(`registry.yaml`·`portfolio.md`·`decisions.md`)은 직접 고치지 않는다.
상태는 `report.py`로 남긴다. 다른 팀의 것은 총괄팀장에게 말로 올린다.

**생성 뒤에는 검증을 붙인다** — 원고·분석·문헌 산출물은 `adversarial-reviewer`(Critic)를 한 번 통과시킨다.
만든 문맥에서 스스로 검증하지 않는다.
{{연구 대상이 LLM인 프로젝트라면 여기에 'LLM 을 판정자로 쓰지 않는다 — 순환논증'이라고 적는다}}

---

이 프로젝트를 실행할 때는 `research-team-orchestrator`, 단계 판단은 `lead-runbook`(유형: {{...}})을 사용하라.

**이 프로젝트의 불변 규칙:**
<!-- 연구책임자가 승인한, 대화창이 임의로 바꾸면 안 되는 것들. 범위·주 결과·사전 고정 항목 등.
     여기 적힌 것은 '지키면 좋은 것'이 아니라 '어기면 연구가 무효가 되는 것'이다. -->
- {{...}}
